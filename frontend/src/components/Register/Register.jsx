/**
 *  Register component are generally responsible for rendering UI elements, receiving props, and displaying information. 
 *  They don't handle complex state logic or business operations but focus on how the UI looks and behaves.
 */