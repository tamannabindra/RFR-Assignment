/**
 * The route file define the various endpoints (URL paths) the application will respond to. 
 * Each route is associated with an HTTP method (GET, POST, PUT, DELETE, etc.)
 */

/**
 * This route file in AuthModule contains set of endpoints of the 
 * application that handle authentication-related tasks.
 * 
 * For example, for user registration  and User login 
 */