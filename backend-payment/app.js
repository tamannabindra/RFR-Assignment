/**
 * This is the main entry point of the applications.
 * This file typically sets up the application server, initializes middleware, initialize the global error handler, defines the base routes, 
 * and starts listening for requests on a specific port.
 */