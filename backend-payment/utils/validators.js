/**
 * Validators refers to a component or a set of rules that are used to ensure that the data being passed from the view (user interface) to the model (data layer) is correct, 
 * properly formatted, and meets all predefined constraints before it is processed or saved. 
 */