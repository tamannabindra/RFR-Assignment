/**
 * This controller file contains the logic that handles incoming HTTP requests, processes data, and returns responses. 
 * The controller acts as an intermediary between the model which represents the data and the business logic) and the view (which is the presentation layer).
 */